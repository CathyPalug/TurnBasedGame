# Rule.md — 이 프로젝트의 절대 규칙

> **AI(Claude)는 작업을 시작하기 전에 항상 이 파일을 먼저 읽고, 아래 규칙을 그대로 따른다.**

---

## 0. 이 프로젝트가 뭔가

2026년도 전국기능경기대회 **게임 개발** 직종 과제를 연습하기 위한 프로젝트다.

- 과제 주제 : **"마을을 위협하는 던전 속 몬스터 퇴치"** 턴제 게임
- 경기 시간 : **16시간**
- 최종 목표 : 대회장에서 **내가 혼자, 처음부터, 16시간 안에 똑같이 다시 만들 수 있는 것**

기준 문서 : **`D:\Downloads\2026_전국_기획서최종수정본.hwpx`**  ← ★최종수정본이 기준★

> ⚠️ `2026_전국_기획서.hwpx` (구버전)이 아니라 **최종수정본**을 봐야 한다.
> 구버전과 다른 점 : 배낭 6칸→**4칸**, 장비도 배낭 보관, 아이템 버리기,
> 랭킹 영구 저장, ESC 일시정지, 설정(BGM/SFX) 화면.

---

## 1. 기획서가 최우선이다

- **무조건 기획서를 참고한다.** 수치(HP, 공격력, MP, 쿨타임, 가격, 경험치 등)는
  기획서에 적힌 값을 **그대로** 쓴다. 마음대로 바꾸지 않는다.
- 기획서에 없는 내용은 만들기 전에 **2번 규칙(질문하기)** 에 따라 먼저 물어본다.

---

## 2. 사용자의 응답이 필요하면 항상 질문한다  ★가장 중요★

아래에 해당하면 **혼자 정하지 말고 반드시 먼저 질문한다.**

- 기획서에 안 적혀 있어서 내가 정해야 하는 것 (예: 색깔, 배치, 연출 방식)
- 갈림길이 있는 것 (예: 3D로 할지 2D UI로 할지, 어떤 에셋을 쓸지)
- 되돌리기 어려운 것 (예: 파일 삭제, 씬 통째로 갈아엎기)
- 두 가지 이상으로 해석되는 지시

질문할 때는 **선택지를 2~4개로 좁혀서** 물어본다. "어떻게 할까요?" 같은 열린 질문은 하지 않는다.

---

## 3. 코드는 "코딩 1주차" 수준으로 쓴다

대회장에서 내가 **외워서 다시 칠 수 있어야 하기 때문**이다.

### 써도 되는 것
- `if` / `else` / `for` / `while`
- `int`, `float`, `bool`, `string`, 배열 `[]`, `List<>`
- 평범한 함수 (`public void 이름()`, `public int 이름()`)
- `class` 하나에 변수와 함수 담기
- 유니티 기본 : `MonoBehaviour`, `Start()`, `Update()`, `public` 변수로 Inspector 연결
- 코루틴(`IEnumerator` + `yield return new WaitForSeconds`) — 단, 주석을 꼭 단다

### 쓰면 안 되는 것
- `abstract`, `virtual`, `override`, `interface` — 상속 계산은 금지
- 프로퍼티 (`public int Atk { get { ... } }`) → 무조건 함수 `GetAtk()` 로
- `Action`, `Func`, 델리게이트, 람다 (`=>`) 로 로직 흐름 만들기
- LINQ (`.Where()`, `.Select()`, `.OrderBy()`)
- `out` / `ref` 파라미터
- 제네릭 직접 만들기 (`class Box<T>`)
- 디자인 패턴 이름이 붙은 구조 (옵저버, 팩토리 등)

### 대신 이렇게 한다
| 어려운 것 | 쉬운 대체 |
|---|---|
| `override` 로 계산 다르게 | 변수(`bonusAtk` 등)에 값을 넣어두고 계산은 한 곳에서 |
| `Action` 콜백 | `int` 상태값 기억했다가 `if` 로 나누기 |
| LINQ 정렬 | `for` 두 번 도는 선택 정렬 |
| `out` 2개 | 작은 `class` 상자 하나 만들어 돌려주기 |
| `switch` | `if` / `else if` |

---

## 4. 주석 규칙

- **모든 주석은 한글로 쓴다.**
- **새로 만든 것에는 반드시 주석을 단다.**
- 파일 맨 위에는 항상 이 파일이 **뭐 하는 파일인지** 요약 블록을 넣는다.
- 계산식에는 **예시 숫자**를 같이 적는다. (예: `// 50 + 15 = 65`)
- 헷갈리기 쉬운 유니티 문법은 그 자리에서 한 줄로 설명한다.
  (예: `// Instantiate = 프리팹을 복사해서 화면에 만드는 함수`)

---

## 5. 에셋 규칙

- **에셋 파일은 절대 지우지 않는다.** (`Assets/` 안의 다운받은 폴더들)
- **에셋 안에 들어있는 스크립트(.cs)는 쓰지 않는다.**
  - 모델, 텍스처, 프리팹(모양), 사운드, 스프라이트, 애니메이션 **리소스만** 가져다 쓴다.
  - 로직은 100% 내가 직접 짠 스크립트로 만든다.
  - 에셋 프리팹을 몬스터로 쓸 때는, 프리팹에 붙어있는 에셋 스크립트를 **떼어내고** 내 스크립트를 붙인다.
- **에셋을 썼으면 `만드는 방법.md` 에 무슨 에셋을 어디에 썼는지 반드시 적는다.**

---

## 6. 문서 규칙

작업하면서 아래 두 문서를 계속 갱신한다.

| 파일 | 내용 |
|---|---|
| `Rule.md` | 이 규칙 파일. 규칙이 바뀌면 여기를 고친다. |
| `만드는 방법.md` | **대회장 재현용 매뉴얼.** 순서대로 따라 하면 똑같이 만들어지게 적는다. 사용한 에셋 목록 포함. |

`만드는 방법.md` 에 꼭 들어가야 할 것
1. 만드는 **순서** (1단계, 2단계 …)
2. 각 단계에서 **만든 파일 / 만든 오브젝트**
3. **어떤 에셋을 어디에 썼는지**
4. Inspector 에 **무엇을 연결해야 하는지**
5. 막히기 쉬운 부분 주의사항

---

## 7. 작업 시작 전 체크리스트

작업을 시작하기 전에 아래를 **매번** 확인한다.

1. [ ] `Rule.md` 를 읽었는가
2. [ ] **MCP(UnityMCP)가 연결되어 있는가** → 연결 안 되어 있으면 **즉시 중단하고 알린다**
3. [ ] 기획서 내용을 확인했는가
4. [ ] 애매한 부분이 있으면 질문했는가 (2번 규칙)

---

## 8. 기능 체크리스트 (기획서 기준)

구현이 끝났는지 판단하는 기준이다.

### UI (배점 15)
- [ ] 메뉴 화면 (게임시작 / 게임소개 / 게임방법 / 랭킹 / 종료)
- [ ] 플레이어 정보 : 레벨, 경험치(`현재/필요` 표기), HP, MP, 스킬, 보관함
- [ ] 적 HP 표시
- [ ] 지도(맵) 표시 + 이동 가능한 경로 표시
- [ ] 랭킹 : 상위 5위 클리어 시간, 이니셜을 **버튼 클릭**으로 입력, 2글자 이상이면 완료 버튼 활성화
- [ ] **랭킹은 게임을 껐다 켜도 저장되어 있어야 한다** (PlayerPrefs)
- [ ] **ESC 일시정지** : 시간이 멈추고, 설정 화면으로 갈 수 있고, ESC 다시 누르면 닫힘
- [ ] **설정 화면** : BGM / SFX 음량을 0~100% 로 조절
- [ ] 풀스크린 모드

### 플레이어 (배점 20)
- [ ] 기본 능력치 : HP 100 / MP 50 / 방어 10 / 공격 20 / 속도 10 / **배낭 4칸** / 크리 10% / 회피 15%
- [ ] 경험치 = 몬스터 경험치 × 몬스터 레벨
- [ ] 골드 = 경험치 획득량 ÷ 2
- [ ] 레벨업 시 HP+20 / MP+10 / 공격+10, 최대 10레벨
- [ ] 레벨 1·2·4·6 에서 스킬 자동 습득
- [ ] 스킬 13종 전부

### 구성요소 (배점 25)
- [ ] 몬스터 10종 (일반 6 / 정예 1 / 보스 3) 능력치 공식대로
- [ ] 몬스터 패턴 : 공격 / **방어(반드시 먼저 발동, 플레이어가 미리 알 수 있어야 함)**
- [ ] 정예 몬스터는 스킬 1개 필수
- [ ] 보스별 스킬 + 페이즈 변화 (2보스 / 3보스)
- [ ] 파괴 광선은 **예고** 표시
- [ ] 장비 11종 (무기 6 / 갑옷 5)
- [ ] 소모품 5종 + 최대 누적 개수 제한
- [ ] 상점(장비/스킬북/아이템 구매) + 휴식(체력 30% 회복)
- [ ] **산 물건은 배낭으로 들어간다. 배낭이 꽉 차 있으면 구매 불가**
- [ ] **전투 중이 아닐 때만** 배낭에서 장비 착용 / 교체 가능
      (착용하면 배낭에서 빠지고, 교체하면 벗은 장비가 배낭으로 돌아온다)
- [ ] **전투 중이 아닐 때만** 배낭에서 아이템 버리기 가능

### 레벨 디자인 (배점 10)
- [ ] 3개 스테이지
- [ ] 스테이지 끝은 반드시 보스방, 스테이지당 보스방 1개
- [ ] 맵에서 이어진 방으로만 이동 가능 (경로 표시)

### 기술적 요소 (배점 20)
- [ ] 크리티컬 2배 / 회피
- [ ] 방어력 감소 공식 (최대 감소는 원래 위력의 10% 아래로 못 감)
- [ ] 속도 기반 턴 순서
- [ ] 효과음 / 배경음악
- [ ] 치트키 F1 ~ F10 전부

### 완성도 (배점 10)
- [ ] 실행 중 에러 / 강제 종료 없음 (에러 나면 그 항목 0점!)
- [ ] 콘솔 에러 0개

---

## 9. 치트키 (기획서 필수)

| 키 | 기능 | 키 | 기능 |
|---|---|---|---|
| F1 | 플레이어 무적 | F6 | 현재 전투의 모든 적 제거 |
| F2 | 공격력 +100 | F7 | 메인화면 |
| F3 | HP 최대 회복 | F8 | 1스테이지 이동 |
| F4 | MP 최대 회복 | F9 | 2스테이지 이동 |
| F5 | 레벨 1 업 | F10 | 3스테이지 이동 |
