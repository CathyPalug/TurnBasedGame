# 만드는 방법 — 대회장 재현 매뉴얼

> **이 문서 하나만 보고 처음부터 똑같이 만들 수 있게** 순서대로 적었다.
> 기준 문서 : `2026_전국_기획서최종수정본.hwpx`
> 작업 규칙 : [`Rule.md`](Rule.md)

---

## 0. 전체 그림

| 항목 | 내용 |
|---|---|
| 장르 | 턴제 RPG (JRPG 방식 — 3D 몬스터 + 하단 커맨드창) |
| 유니티 버전 | 6000.3.17f1 (URP) |
| 씬 | `Assets/Decrepit Dungeon LITE/EXAMPLE/Demo_Scene.unity` |
| 스크립트 | `Assets/Scripts/` 아래 **17개** (약 4,400줄) |
| 화면 해상도 기준 | 1920 × 1080 |
| 텍스트 | 레거시 `UI.Text` + 맑은 고딕 폰트 |

### 만드는 순서 요약
```
1단계  폴더 만들기            (5분)
2단계  한글 폰트 넣기          (3분)
3단계  스크립트 17개 작성      (5~6시간)
4단계  몬스터 프리팹 10개 만들기 (40분)
5단계  전투장(3D) 만들기       (30분)
6단계  UI 부품 프리팹 5개      (30분)
7단계  Canvas 화면 12개 만들기  (3시간)
8단계  Inspector 연결          (40분)
9단계  테스트 + 버그 잡기       (2시간)
────────────────────────────────
                        합계 약 13~14시간
```

---

## 1단계 — 폴더 만들기

```
Assets/
 ├ Scripts/          ← 내가 만든 스크립트 (여기만 코딩)
 ├ Fonts/            ← 한글 폰트
 ├ Prefabs/
 │   ├ Battle/       ← 몬스터 전투용 프리팹 10개
 │   └ UI/           ← UI 부품 프리팹 5개
 └ (에셋 폴더들)      ← 건드리지 않는다
```

---

## 2단계 — 한글 폰트 넣기 ★꼭 먼저 할 것★

레거시 `UI.Text`의 기본 폰트는 **한글이 안 나온다.**

1. 탐색기에서 `C:\Windows\Fonts\malgun.ttf` 복사
2. `Assets/Fonts/` 에 붙여넣기
3. 앞으로 만드는 **모든 Text 의 Font 칸에 malgun 을 지정**

> ⚠️ **가장 많이 하는 실수**
> Text 의 `Vertical Overflow` 가 기본값 `Truncate` 라서,
> 글자 상자가 글씨보다 조금이라도 낮으면 **글자가 통째로 안 보인다.**
> → 만드는 모든 Text 를 **`Overflow`** 로 바꿔둘 것.

---

## 3단계 — 스크립트 17개

`Assets/Scripts/` 에 아래 순서대로 만든다. (앞 파일을 뒤 파일이 쓰므로 순서 중요)

| 순서 | 파일 | 하는 일 |
|---|---|---|
| 1 | `GameData.cs` | **모든 숫자표** — enum, 데이터 클래스, 스킬13/장비11/아이템5/몬스터10/몬스터스킬7 |
| 2 | `Unit.cs` | 플레이어·몬스터 **공통** (체력, 능력치, 버프 배열, 피해/회복) |
| 3 | `Player.cs` | MP, 경험치, 골드, 장비, 배낭 4칸, 스킬 |
| 4 | `Monster.cs` | 몬스터 AI (행동 예고 / 방어 / 스킬 / 보스 페이즈) |
| 5 | `BattleManager.cs` | 턴 진행 + **피해 계산 공식** + 대상 고르기 |
| 6 | `GameManager.cs` | 새 게임 / 게임오버 / 클리어 / **치트키 F1~F10** |
| 7 | `UIManager.cs` | 화면 전환 + **ESC 일시정지** + 설정(BGM/SFX) |
| 8 | `StageManager.cs` | 3스테이지 지도 (방 버튼·길을 **코드가 자동 생성**) |
| 9 | `AudioManager.cs` | BGM/SFX + 음량 저장 |
| 10 | `RankingManager.cs` | 랭킹 5위 **PlayerPrefs 영구 저장** |
| 11 | `BattleUI.cs` | 커맨드창 / 스킬·아이템·대상 목록 / 전투기록 / 적 정보 |
| 12 | `StatusUI.cs` | 레벨·HP·MP·경험치·골드 표시 |
| 13 | `ShopUI.cs` | 상점 목록 / 구매 / 휴식 |
| 14 | `BagUI.cs` | 배낭 4칸 / 착용 / 버리기 |
| 15 | `RankingUI.cs`, `NameInputUI.cs`, `DamagePopup.cs` | 랭킹표 / 이니셜 입력 / 피해 숫자 |

### 꼭 지킬 코딩 규칙 (Rule.md 3번)
- `abstract` / `virtual` / `override` / `interface` **안 씀**
- 프로퍼티 대신 **함수** (`GetFinalAtk()`)
- `Action`·람다·LINQ·`out` **안 씀**
- `switch` 대신 `if / else if`
- 정렬은 **for 두 번 도는 선택 정렬**

### 특히 헷갈리는 3가지

**① 버프는 클래스 대신 배열 2개로**
```csharp
public float[] effectValue = new float[(int)EffectType.Count];
public int[]   effectTurn  = new int[(int)EffectType.Count];
// 쓸 때 : effectTurn[(int)EffectType.AtkUp]
```

**② 대상 고르기는 "숫자 기억"으로** (콜백 금지)
```csharp
const int PICK_NONE = 0, PICK_ATTACK = 1, PICK_SKILL = 2;
int pickMode;        // 지금 왜 고르는 중인지
int pickSkillNum;    // 고른 뒤 쓸 스킬
```

**③ 반복문 변수는 반드시 복사해서 버튼에 넘기기**
```csharp
int 번호 = i;                                   // ★ 이 줄이 없으면 전부 마지막 번호로 동작
버튼.onClick.AddListener(delegate { 함수(번호); });
```

---

## 4단계 — 몬스터 프리팹 10개

**에셋 : `LowPoly Fantasy Monsters Pack Ver1.0_Demo`**

`GameData.Monsters` 배열 순서와 **똑같이** 10개를 만든다.

| 번호 | 프리팹 이름 | 원본 모델 | 키(m) |
|---|---|---|---|
| 0 | M00_고블린 | goblin_01 | 1.9 |
| 1 | M01_늑대 | wolf_02 | 1.5 |
| 2 | M02_홉고블린 | Hobgoblin_002 | 2.3 |
| 3 | M03_사나운늑대 | wolf_02 | 1.7 |
| 4 | M04_트롤 | troll_02_01 | 2.6 |
| 5 | M05_광폭한홉고블린 | Hobgoblin_002 | 2.6 |
| 6 | M06_정예트롤 | troll_02_01 | 3.0 |
| 7 | M07_보스_고블린군주 | goblin_01 | 3.0 |
| 8 | M08_보스_홉고블린대장 | Hobgoblin_002 | 3.4 |
| 9 | M09_보스_고대트롤왕 | troll_02_01 | 4.2 |

### 만드는 방법 (하나당)
1. 빈 GameObject 만들고 이름을 `M00_고블린` 으로
2. 원본 모델 프리팹을 그 **자식**으로 넣고 이름을 `Model` 로
3. `Model` 의 Rotation Y = **90** (플레이어 쪽을 보게)
4. **★ `Model` 안의 스크립트를 전부 제거** — `Move` 스크립트가 붙어 있다 (Rule 5)
5. **★ `Model` 안의 Particle System 자식들을 전부 삭제**
   → 이 파티클은 Built-in 셰이더라 URP 에서 **캐릭터를 밝은 색으로 덮어버린다**
6. 크기를 위 표의 키에 맞게 Scale 조정, 발이 y=0 에 닿게
7. 루트에 내가 만든 **`Monster` 스크립트**를 추가하고 `dataNum` 을 번호로
8. 자식으로 빈 오브젝트 `HitPoint` 를 만들어 머리 위(키+0.4)에 두고 `hitPoint` 에 연결
9. `Assets/Prefabs/Battle/` 에 프리팹으로 저장

---

## 5단계 — 전투장(3D)

**에셋 : `Decrepit Dungeon LITE`**

원본 데모 던전(`Demo Scene` 오브젝트)은 **비활성화**하고 (포인트 라이트 59개라 무겁다),
같은 에셋 조각으로 작은 전투장을 짓는다.

| 오브젝트 | 내용 |
|---|---|
| `Arena` (y = −50) | 전투장 루트. 던전과 겹치지 않게 아래에 둔다 |
| 바닥 | `Floors/Floor_A` 를 6 × 4 로 깐다 (한 장 5.8 크기) |
| 뒷벽 | `Walls/Wall_A` 를 z = 8.5 에 일렬로, Rotation Y = 90 |
| 기둥 | `Pillars/Pillar_A` 4개를 z = 7.6 에 |
| 조명 | Directional(1.15) + Point(주황, range 26) |
| `MonsterSlots` | 빈 오브젝트 4개. x = −4.6 / −2.9 / −1.2 / 0.5 |
| `Player` | 아래 참고. x = 4.3 (오른쪽), Rotation Y = 270 |

### 플레이어 만들기
**에셋 : `ExplosiveLLC / Warrior Pack Bundle 2 FREE` 의 `Knight Warrior`**
1. 빈 오브젝트 `Player` 를 만들고 Knight 를 자식 `Model` 로
2. **★ Model 의 스크립트 5개를 전부 제거**
   (`GUIControls`, `WarriorController`, `WarriorMovementController`,
    `SuperCharacterController`, `WarriorInputController`) — Rule 5
3. Collider, Rigidbody 도 제거 (안 움직이므로 필요 없다)
4. 루트에 **`Player` 스크립트** 추가, `HitPoint`(y=2.9) 연결

### 카메라
| 항목 | 값 |
|---|---|
| Position | (−0.2, −46.3, −8.2) |
| Rotation | (13, 0, 0) |
| Field of View | 46 |
| Clear Flags | Solid Color (어두운 남색) |

### ★ URP 머티리얼 변환 (안 하면 캐릭터가 분홍색)
에셋 머티리얼이 Built-in Standard 셰이더라 URP 에서 **분홍색**으로 보인다.
- 메뉴 `Window → Rendering → Render Pipeline Converter` 로 일괄 변환
- 또는 머티리얼마다 Shader 를 `Universal Render Pipeline/Lit` 로 바꾸고
  원래 `_MainTex` 를 `_BaseMap` 에 다시 넣어준다

### ★ Run In Background 켜기
`Edit → Project Settings → Player → Resolution → Run In Background` 체크
(창이 비활성일 때 게임이 멈추는 것을 막는다)

---

## 6단계 — UI 부품 프리팹 5개

**에셋 : `GUI_Parts`** (스프라이트는 전부 **어두운 색** → 글자는 **밝은 색**으로!)

| 프리팹 | 구성 | 쓰는 곳 |
|---|---|---|
| `UI_ListButton` | Image(`button`) + Button + 자식 Text | 스킬/아이템/대상/상점 목록 |
| `UI_EnemyInfo` | Image(어두운 상자) + Text + Slider(`Hp_frame`/`Hp_line`) | 적 정보 한 줄 |
| `UI_RoomButton` | Image(`lil_roundframe`) + Button + Text | 지도 방 |
| `UI_MapLine` | Image(연한 갈색) | 지도의 길(선) |
| `UI_DamageText` | Text(굵게 40) + Outline | 피해 숫자 |

> 5개 모두 Text 의 **Vertical Overflow = Overflow** 로!

---

## 7단계 — Canvas 화면 12개

**Canvas 설정** : Screen Space - Overlay / Scale With Screen Size / **1920×1080** / Match 0.5

### 메인 화면 (한 번에 하나만 켜짐)
| 화면 | 내용 |
|---|---|
| `MenuPanel` | 제목 + 버튼 6개 (게임시작/게임소개/게임방법/랭킹/설정/종료) |
| `IntroPanel` | 게임 소개 글 + 뒤로 |
| `HowToPanel` | 게임 방법 글 + 뒤로 |
| `RankingPanel` | 1~5위 줄 + 초기화 + 뒤로 |
| `MapPanel` | STAGE 제목 + `MapArea`(빈 오브젝트) + 상태창 + 배낭 버튼 |
| `BattlePanel` | **배경 없음** (3D 가 보여야 함) |
| `ShopPanel` | 상점 목록 + 골드/배낭/안내 + 휴식/배낭/나가기 |

### BattlePanel 배치 (1920×1080 기준, 가운데가 0,0)
| 요소 | 위치 | 크기 |
|---|---|---|
| `TopBar` (STAGE 표시) | (0, 470) | 560×66 |
| `EnemyBox` → 안에 `EnemyContent` | (−660, 240) | 540×340 |
| `StatusBox` (StatusUI) | (−645, −388) | 600×260 |
| `LogBox` (Text 5줄) | (0, −388) | 700×260 |
| `CommandWindow` (버튼 4개) | (645, −388) | 470×260 |
| `ListWindow` (스킬/아이템/대상) | (0, 60) | 720×620 |

- `EnemyContent`, `ListWindow/Viewport/Content` 에는 **Vertical Layout Group** 을 넣고
  `Child Control / Force Expand` 는 **전부 끈다**
- `Content` 에는 **Content Size Fitter**(Vertical = Preferred), `Viewport` 에는 **Mask + Scroll Rect**

### 겹쳐 뜨는 화면
| 화면 | 내용 |
|---|---|
| `PausePanel` | 계속하기 / 설정 / 메인 메뉴 |
| `OptionPanel` | BGM·SFX 슬라이더(0~100) + 값 표시 + 닫기 |
| `BagPanel` | 배낭 4칸 + 무기/갑옷 + 사용/버리기/닫기 |
| `NamePanel` | 클리어시간 + 입력창 + **A~Z 버튼 26개** + 지우기/완료 |
| `ResultPanel` | 결과 글 + 확인 |
| `PopupLayer` | 빈 오브젝트. 피해 숫자가 여기에 생긴다 |

---

## 8단계 — Inspector 연결표

### Managers (빈 오브젝트 하나에 6개 스크립트)
| 스크립트 | 연결할 것 |
|---|---|
| `GameManager` | player |
| `BattleManager` | player, battleUI, monsterSlots(4), **monsterPrefabs(10개, 순서 중요!)** |
| `StageManager` | mapArea, roomButtonPrefab, linePrefab, stageTitleText, battleStageText |
| `UIManager` | 패널 12개 + resultText + bgm/sfx 슬라이더 + 값 Text 2개 |
| `AudioManager` | (소리 파일들 — 없으면 비워둬도 됨) |
| `DamagePopup` | popupParent(PopupLayer), popupPrefab, worldCamera, hitEffect, criticalEffect |

### 화면에 붙이는 스크립트
| 스크립트 | 붙이는 곳 |
|---|---|
| `BattleUI` | BattlePanel |
| `StatusUI` | MapPanel/StatusBox, BattlePanel/StatusBox (**2개**) |
| `ShopUI` | ShopPanel |
| `BagUI` | BagPanel |
| `RankingUI` | RankingPanel |
| `NameInputUI` | NamePanel |

### 버튼 OnClick 직접 연결하는 것만
(나머지는 스크립트가 `Awake`/`Start` 에서 스스로 연결한다)

| 버튼 | 연결 |
|---|---|
| 메뉴 6개 | `OnClickStart` / `ShowIntro` / `ShowHowTo` / `ShowRanking` / `ShowOption` / `OnClickQuit` |
| 뒤로 3개 | `OnClickBack` |
| 배낭 버튼(지도·전투) | `ToggleBag` |
| 일시정지 3개 | `OnClickResume` / `ShowOption` / `OnClickPauseToMenu` |
| 설정 닫기 | `CloseOption` |
| 결과 확인 | `OnClickResultOk` |

---

## 9단계 — 확인 체크리스트

- [ ] 콘솔 에러 **0개** (에러 나면 그 항목 0점!)
- [ ] 시작 능력치 : HP100 / MP50 / 공격30(20+롱소드10) / 방어15(10+천갑옷5) / 크리10% / 회피15%
- [ ] 1레벨에 베기·가르기 자동 습득
- [ ] 몬스터 HP·공격력이 `기본값 + 레벨×증가량` 과 일치
- [ ] 몬스터 방어 감소율 = `50 + 레벨×3` %
- [ ] 적이 방어할 때 적 정보에 **■[방어]** 미리 표시
- [ ] 파괴 광선 예고 **◆[파괴 광선 준비!]** 표시
- [ ] 경험치 = 몬스터경험치 × 레벨, 골드 = 그 절반
- [ ] 상점에서 산 물건이 배낭으로, 배낭 꽉 차면 구매 불가
- [ ] 전투 중 장비 교체 / 버리기 **불가**
- [ ] ESC 일시정지 → 시간 멈춤 → 다시 ESC 닫힘
- [ ] 랭킹이 게임을 껐다 켜도 남아있음
- [ ] **치트키 F1~F10 전부 동작**

---

## 10. 사용한 에셋 목록 ★

기획서에 적힌 공식 리소스 중 **현재 프로젝트에 받아둔 것만** 사용했다.
**에셋의 스크립트(.cs)는 하나도 쓰지 않았다** (Rule 5) — 모델·텍스처·사운드·스프라이트만 사용.

| 에셋 이름 | 어디에 썼나 | 스크립트 사용 |
|---|---|---|
| **LowPoly Fantasy Monsters Pack Ver1.0_Demo** | 몬스터 10종 3D 모델 (고블린/늑대/홉고블린/트롤) | ❌ `Move` 제거함 |
| **ExplosiveLLC — Warrior Pack Bundle 2 FREE** | 플레이어 캐릭터 (Knight Warrior) | ❌ 스크립트 5개 제거함 |
| **Decrepit Dungeon LITE** | 전투장 바닥·벽·기둥 (Floor_A / Wall_A / Pillar_A), 씬 파일 | ❌ 없음 |
| **GUI_Parts** | UI 전체 (버튼 `button`, 배경 `big_background`·`mid_background`·`Mini_background`, 틀 `Frame_mid`, 체력바 `Hp_frame`·`Hp_line`, 방 버튼 `lil_roundframe`) | ❌ 없음 |
| **Matthew Guz — Hits Effects FREE** | 타격 이펙트 (`Basic Hit`), 크리티컬 이펙트 (`Fire Hit`) | ❌ 없음 |
| **Alebardium — Bloodlines UI** | 버튼 클릭 효과음 (`Click Button SFX.wav`) | ❌ 없음 |
| **맑은 고딕** (Windows 기본 폰트) | 모든 한글 텍스트 | — |

### 아직 안 받은 공식 리소스 (나중에 넣으면 바로 동작)
`AudioManager` 의 Inspector 칸에 **끌어다 놓기만** 하면 소리가 난다.

| 종류 | 기획서 리소스 | 넣을 칸 |
|---|---|---|
| BGM | Fantasy BGM Free Pack / Metal Gothic Epic / Sharp Showdown | `menuBgm`, `battleBgm`, `bossBgm` |
| SFX | RPG Essentials Sound Effects / 8bit SFX | `hitSfx`, `criticalSfx`, `healSfx`, `levelUpSfx` |
| 아이콘 | RPG Inventory Icons / 500 Free Skill Icons / Bronze Icon Pack | 목록 버튼에 Image 추가 |
| 모델 | Medieval Weapons Starter Kit / Stylized Potions | 장비·포션 표시용 |

---

## 11. 막히기 쉬운 곳 (내가 실제로 겪은 것)

| 증상 | 원인 | 해결 |
|---|---|---|
| 캐릭터가 **분홍색** | Built-in 셰이더 + URP | 머티리얼을 `URP/Lit` 로 변환 |
| 캐릭터가 **밝은 하늘색으로 뭉개짐** | 에셋에 딸린 **파티클**이 URP 미지원 | 프리팹에서 Particle System 자식 **삭제** |
| **글자가 아예 안 보임** | Text 의 `Vertical Overflow = Truncate` + 상자가 낮음 | **Overflow** 로 변경 |
| 글자가 배경에 묻힘 | GUI_Parts 스프라이트가 **전부 어두움** | 글자를 **밝은 크림색**으로 |
| 🛡 ⚠ 같은 **이모지가 안 나옴** | 한글 폰트에 그림 문자가 없음 | `■[방어]` `◆[준비!]` 처럼 **글자로** 표시 |
| 목록 버튼이 **전부 마지막 것만** 실행됨 | 반복문 변수를 그대로 넘김 | `int 번호 = i;` 로 **복사** 후 사용 |
| 목록 창 닫기가 **무한 반복**됨 | `CloseList` ↔ `CancelPick` 서로 호출 | `CloseList` 는 **닫기만**, 취소는 따로 |
| 플레이 중인데 **시간이 안 감** | Unity 창이 비활성 | **Run In Background** 켜기 |
| 장비를 껴도 **공격력이 안 오름** | 보너스 재계산 누락 | `Equip` / 치트 뒤에 `UpdateBonus()` 호출 |

---

## 12. 지금 상태

✅ 컴파일 에러 0개 / 실행 에러 0개
✅ 메뉴 · 지도 · 전투 · 상점 · 배낭 · 랭킹 · 일시정지 · 설정 전부 동작
✅ 기획서 수치 검증 완료 (능력치 · 피해 공식 · 방어 감소율 · 경험치/골드)

⬜ 남은 작업 : BGM/SFX 파일 연결, 아이템 아이콘, 3스테이지 보스 실전 플레이 테스트
